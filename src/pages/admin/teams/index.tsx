import DashboardLayout from "@/components/layouts/AdminLayout";

const Teams = () => {
  return <div>Teams</div>;
};

Teams.getLayout = (page: any) => <DashboardLayout>{page}</DashboardLayout>;

export default Teams;
